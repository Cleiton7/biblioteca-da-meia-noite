# Variáveis de ambiente utilizadas pelo docker-compose.yml
# Copie este arquivo para ".env" e ajuste os valores conforme necessário

# Nome do banco de dados MySQL
DB_NAME=biblioteca_meia_noite

# Senha do usuário root do MySQL (usada também pelo backend para conectar)
DB_PASSWORD=admin
